import { useGetChannelAnalytics, useGetCreatorFinance, useGetMe, useListVideos } from '@workspace/api-client-react';
import { Link } from 'wouter';
import { Activity, ArrowUpRight, BarChart3, CircleAlert, CircleDot, Clapperboard, Eye, Film, Landmark, Library, Loader2, Radio, Settings, Sparkles, Tv, UploadCloud, Users, Wallet } from 'lucide-react';

function formatDuration(seconds: number | null | undefined) {
  const value = Math.max(0, seconds ?? 0);
  const hours = Math.floor(value / 3600);
  const minutes = Math.floor((value % 3600) / 60);
  return hours > 0 ? `${hours}h ${minutes}m` : `${minutes}m`;
}

function MetricCard({ label, value, detail, icon: Icon, accent = false }: { label: string; value: string | number; detail: string; icon: typeof Activity; accent?: boolean }) {
  return <article className={`rounded-2xl border p-4 ${accent ? 'border-primary/25 bg-primary/[0.06]' : 'border-white/[0.08] bg-white/[0.025]'}`}><div className="flex items-start justify-between gap-3"><div><p className={`text-2xl font-black tracking-tight ${accent ? 'text-primary' : 'text-white'}`}>{value}</p><p className="mt-1 text-[10px] font-black uppercase tracking-[0.16em] text-white/45">{label}</p></div><span className={`flex h-9 w-9 items-center justify-center rounded-xl ${accent ? 'bg-primary/15 text-primary' : 'bg-white/[0.06] text-white/45'}`}><Icon className="h-4 w-4" /></span></div><p className="mt-3 text-xs leading-relaxed text-white/40">{detail}</p></article>;
}

function ActionCard({ href, icon: Icon, eyebrow, title, detail, tone = 'primary' }: { href: string; icon: typeof Radio; eyebrow: string; title: string; detail: string; tone?: 'primary' | 'red' | 'white' }) {
  const toneClass = tone === 'red' ? 'border-red-300/20 bg-red-500/[0.06] hover:border-red-300/50' : tone === 'white' ? 'border-white/[0.1] bg-white/[0.025] hover:border-white/25' : 'border-primary/25 bg-primary/[0.055] hover:border-primary/55';
  const iconClass = tone === 'red' ? 'bg-red-500/15 text-red-100' : tone === 'white' ? 'bg-white/[0.07] text-white/70' : 'bg-primary/15 text-primary';
  return <Link href={href} className={`group block rounded-2xl border p-4 transition hover:-translate-y-0.5 ${toneClass}`}><div className="flex items-start justify-between gap-3"><span className={`flex h-10 w-10 items-center justify-center rounded-xl ${iconClass}`}><Icon className="h-5 w-5" /></span><ArrowUpRight className="h-4 w-4 text-white/30 transition group-hover:text-primary" /></div><p className="mt-5 text-[10px] font-black uppercase tracking-[0.16em] text-white/40">{eyebrow}</p><h2 className="mt-1 text-base font-black text-white">{title}</h2><p className="mt-2 text-xs leading-relaxed text-white/45">{detail}</p></Link>;
}

export default function DashboardStudio() {
  const meQuery = useGetMe();
  const channel = meQuery.data?.channel;
  const channelId = channel?.id ?? 0;
  const videosQuery = useListVideos({ channelId }, { query: { enabled: Boolean(channelId) } });
  const analyticsQuery = useGetChannelAnalytics(channelId, { query: { enabled: Boolean(channelId) } });
  const financeQuery = useGetCreatorFinance({ query: { enabled: Boolean(channelId) } });

  if (meQuery.isLoading) return <div className="flex min-h-[65vh] items-center justify-center"><Loader2 className="h-8 w-8 animate-spin text-primary" /></div>;

  if (!channel) {
    return <main className="relative z-10 mx-auto flex min-h-[70vh] max-w-3xl items-center px-4 py-8 sm:px-6"><section className="w-full overflow-hidden rounded-3xl border border-primary/20 bg-[radial-gradient(circle_at_top_right,rgba(51,238,196,0.17),transparent_35%),linear-gradient(130deg,rgba(91,70,255,0.14),rgba(8,10,15,0.98)_60%)] p-6 sm:p-10"><span className="flex h-12 w-12 items-center justify-center rounded-2xl border border-primary/30 bg-primary/10 text-primary"><Radio className="h-6 w-6" /></span><p className="mt-6 text-[11px] font-black uppercase tracking-[0.2em] text-primary">Creator Studio</p><h1 className="mt-2 max-w-xl text-3xl font-black tracking-tight text-white sm:text-5xl">Build your Kryv creator home.</h1><p className="mt-4 max-w-2xl text-sm leading-relaxed text-white/60 sm:text-base">Create your channel first. It unlocks Live broadcast controls, Watch publishing, creator analytics, public profile identity, and crypto-denominated creator finance records when confirmed settlement exists.</p><div className="mt-7 flex flex-col gap-3 sm:flex-row"><Link href="/dashboard/live" className="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl bg-primary px-5 text-sm font-black text-primary-foreground transition hover:bg-primary/90"><Radio className="h-4 w-4" /> Set up your channel</Link><Link href="/creators" className="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl border border-white/[0.16] bg-black/20 px-5 text-sm font-black text-white/75 transition hover:border-primary/45 hover:text-primary"><Users className="h-4 w-4" /> Explore creators</Link></div></section></main>;
  }

  const videos = videosQuery.data ?? [];
  const readyVideos = videos.filter((video) => video.uploadStatus === 'ready');
  const processingVideos = videos.filter((video) => video.uploadStatus === 'waiting' || video.uploadStatus === 'processing');
  const totalViews = videos.reduce((sum, video) => sum + video.viewCount, 0);
  const analytics = analyticsQuery.data;
  const finance = financeQuery.data;
  const payoutSummary = finance?.payoutRequests.length
    ? finance.payoutRequests[0]
    : null;

  return <main className="relative z-10 mx-auto w-full max-w-[1600px] px-4 pb-[calc(1.5rem+env(safe-area-inset-bottom))] pt-5 sm:px-6 sm:py-8 lg:px-8 lg:py-10"><section className="relative overflow-hidden rounded-3xl border border-white/[0.08] bg-[#0b0e14] p-5 sm:p-8 lg:p-10"><div className="pointer-events-none absolute -right-16 -top-24 h-72 w-72 rounded-full bg-primary/20 blur-3xl" /><div className="pointer-events-none absolute -bottom-28 left-1/3 h-64 w-64 rounded-full bg-fuchsia-400/10 blur-3xl" /><div className="relative flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><div className="min-w-0 max-w-3xl"><div className="flex flex-wrap items-center gap-2"><span className="inline-flex items-center gap-2 rounded-full border border-primary/30 bg-primary/10 px-3 py-1 text-[10px] font-black uppercase tracking-[0.18em] text-primary"><Sparkles className="h-3.5 w-3.5" /> Creator Studio</span><span className={`inline-flex items-center gap-1.5 rounded-full border px-3 py-1 text-[10px] font-black uppercase tracking-[0.15em] ${channel.isLive ? 'border-red-300/30 bg-red-500/15 text-red-100' : 'border-white/[0.1] bg-white/[0.04] text-white/45'}`}><CircleDot className={`h-3.5 w-3.5 ${channel.isLive ? 'text-red-400' : 'text-white/35'}`} /> {channel.isLive ? 'Live now' : 'Offline'}</span></div><h1 className="mt-4 truncate text-3xl font-black tracking-tight text-white sm:text-5xl">{channel.displayName}</h1><p className="mt-3 max-w-2xl text-sm leading-relaxed text-white/60 sm:text-base">Your operating home for live broadcasting, Watch publishing, creator performance, profile settings, and crypto-only creator finance.</p></div><div className="grid grid-cols-2 gap-2 sm:flex"><Link href="/dashboard/live" className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-primary px-4 text-sm font-black text-primary-foreground transition hover:bg-primary/90"><Radio className="h-4 w-4" /> Go Live</Link><Link href={`/profile/${channel.slug}`} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl border border-white/[0.14] bg-black/25 px-4 text-sm font-black text-white/75 transition hover:border-primary/45 hover:text-primary"><Users className="h-4 w-4" /> Public profile</Link></div></div></section>

    <section className="mt-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Followers" value={analyticsQuery.isLoading ? '—' : analytics?.followerCount.toLocaleString() ?? 'Unavailable'} detail="Current channel followers from the Live channel record." icon={Users} /><MetricCard label="Live sessions" value={analyticsQuery.isLoading ? '—' : analytics?.totalStreams ?? 'Unavailable'} detail={analytics ? `Recorded over the last ${analytics.periodDays} days.` : 'Authoritative rolling analytics are unavailable.'} icon={Radio} accent /><MetricCard label="Watch views" value={videosQuery.isLoading ? '—' : totalViews.toLocaleString()} detail="Current views across this creator’s Watch releases." icon={Eye} /><MetricCard label="Ready releases" value={videosQuery.isLoading ? '—' : readyVideos.length} detail={videosQuery.isError ? 'Watch inventory is temporarily unavailable.' : `${processingVideos.length} release${processingVideos.length === 1 ? '' : 's'} processing.`} icon={Film} /></section>

    <section className="mt-8"><div className="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[11px] font-black uppercase tracking-[0.18em] text-primary">Creator control tower</p><h2 className="mt-1 text-2xl font-black text-white sm:text-3xl">Create, publish, and operate</h2><p className="mt-2 max-w-2xl text-sm leading-relaxed text-white/45">Each action continues into an existing control surface. Cinema is intentionally excluded: its catalog remains owner and production controlled.</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><ActionCard href="/dashboard/live" icon={Radio} eyebrow="Live control" title={channel.isLive ? 'Manage your live room' : 'Set up your next stream'} detail="Broadcast credentials, stream health, creator chat controls, and live engagement." tone="red" /><ActionCard href="/dashboard/watch" icon={UploadCloud} eyebrow="Watch publishing" title="Add a Watch release" detail="Upload a FastPix video or add a rights-cleared official YouTube embed." /><ActionCard href="/dashboard/live?tab=analytics" icon={Activity} eyebrow="Creator analytics" title="Inspect channel activity" detail="Review streams, chat, current audience, completed tips, and active subscriptions." tone="white" /><ActionCard href="/dashboard/live?tab=revenue" icon={Wallet} eyebrow="Creator finance" title="Review crypto balances" detail="View asset-separated creator liabilities, destination review, and gated payout status." tone="white" /><ActionCard href="/dashboard/live?tab=settings" icon={Settings} eyebrow="Creator identity" title="Edit channel settings" detail="Update the public channel profile, social destinations, chat controls, and alert preferences." tone="white" /></div></section>

    <section className="mt-8 grid gap-5 xl:grid-cols-[1.1fr_0.9fr]"><article className="rounded-2xl border border-white/[0.08] bg-black/25 p-5 sm:p-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex items-center gap-2 text-primary"><BarChart3 className="h-5 w-5" /><h2 className="text-lg font-black text-white">Channel performance</h2></div><p className="mt-1 text-xs leading-relaxed text-white/45">All measurements are from the channel analytics endpoint. No viewer, watch-time, or revenue estimates are added.</p></div><Link href="/dashboard/live?tab=analytics" className="inline-flex min-h-10 w-fit items-center gap-1.5 text-xs font-black text-primary hover:text-white">Open analytics <ArrowUpRight className="h-3.5 w-3.5" /></Link></div>{analyticsQuery.isLoading ? <div className="flex min-h-52 items-center justify-center"><Loader2 className="h-6 w-6 animate-spin text-primary" /></div> : analyticsQuery.isError || !analytics ? <div className="mt-5 rounded-xl border border-red-300/20 bg-red-400/[0.05] p-4 text-sm text-red-100"><p className="font-black">Channel analytics are temporarily unavailable</p><p className="mt-1 text-xs leading-relaxed text-red-100/70">Kryv cannot present a partial performance snapshot while the channel analytics query is unavailable.</p><button type="button" onClick={() => analyticsQuery.refetch()} className="mt-4 inline-flex min-h-10 items-center rounded-xl border border-red-200/25 bg-red-200/[0.08] px-4 text-xs font-black text-red-50 transition hover:bg-red-200/[0.14]">Retry analytics</button></div> : <div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-3"><MetricCard label="Time live" value={formatDuration(analytics.totalStreamSeconds)} detail={`${analytics.totalStreams} recorded sessions`} icon={Radio} /><MetricCard label="Peak viewers" value={analytics.peakViewers.toLocaleString()} detail={`${analytics.averageViewers.toLocaleString()} average viewers`} icon={Users} accent /><MetricCard label="Chat messages" value={analytics.totalChatMessages.toLocaleString()} detail="Recorded channel chat activity." icon={Activity} /><MetricCard label="Completed tips" value={analytics.completedTipCount.toLocaleString()} detail="Count only; assets are never aggregated." icon={Landmark} /><MetricCard label="Active members" value={analytics.activeSubscriptionCount.toLocaleString()} detail="Current active subscription records." icon={Sparkles} /><MetricCard label="Current audience" value={analytics.currentViewerCount.toLocaleString()} detail={analytics.isLive ? 'Live audience right now.' : 'Channel is currently offline.'} icon={Eye} /></div>}</article>

      <article className="rounded-2xl border border-white/[0.08] bg-black/25 p-5 sm:p-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex items-center gap-2 text-primary"><Landmark className="h-5 w-5" /><h2 className="text-lg font-black text-white">Creator finance</h2></div><p className="mt-1 text-xs leading-relaxed text-white/45">Balances stay separated by asset and come from immutable creator balance records after confirmed settlement. Eligible tips and memberships allocate 95% to the creator and 5% to Kryv; any provider checkout commission remains separate and client-borne.</p></div><Link href="/dashboard/live?tab=revenue" className="inline-flex min-h-10 w-fit items-center gap-1.5 text-xs font-black text-primary hover:text-white">Open finance <ArrowUpRight className="h-3.5 w-3.5" /></Link></div>{financeQuery.isLoading ? <div className="flex min-h-52 items-center justify-center"><Loader2 className="h-6 w-6 animate-spin text-primary" /></div> : financeQuery.isError || !finance ? <div className="mt-5 rounded-xl border border-red-300/20 bg-red-400/[0.05] p-4 text-sm text-red-100"><p className="font-black">Creator finance is temporarily unavailable</p><p className="mt-1 text-xs leading-relaxed text-red-100/70">Kryv cannot safely show creator balances or payout state while the finance query is unavailable.</p><button type="button" onClick={() => financeQuery.refetch()} className="mt-4 inline-flex min-h-10 items-center rounded-xl border border-red-200/25 bg-red-200/[0.08] px-4 text-xs font-black text-red-50 transition hover:bg-red-200/[0.14]">Retry finance</button></div> : <><div className="mt-5 grid gap-3 sm:grid-cols-2">{finance.balances.length ? finance.balances.map((balance) => <div key={balance.currency} className="rounded-xl border border-white/[0.07] bg-white/[0.02] p-3"><div className="flex items-center justify-between gap-3"><span className="font-black text-white">{balance.currency}</span><span className="text-sm font-black text-primary">{balance.availableAmount}</span></div><p className="mt-1 text-[10px] font-bold uppercase tracking-wider text-white/35">Available creator liability</p><div className="mt-3 flex justify-between gap-3 text-[10px] text-white/45"><span>Pending {balance.pendingAmount}</span><span>Held {balance.heldAmount}</span></div></div>) : <div className="sm:col-span-2 rounded-xl border border-dashed border-white/[0.1] p-4 text-xs leading-relaxed text-white/40">No settled creator balance rows have been recorded. Kryv does not create estimated earnings or synthetic currency totals.</div>}</div><div className="mt-5 rounded-xl border border-white/[0.07] bg-white/[0.02] p-3"><div className="flex items-start gap-3"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary"><Wallet className="h-4 w-4" /></span><div><p className="text-xs font-black text-white">Payout operations</p><p className="mt-1 text-[10px] leading-relaxed text-white/35">Allocation is recorded only after signed provider confirmation in the original crypto asset; this Studio does not turn USD reference quotes into earnings.</p><p className="mt-2 text-xs leading-relaxed text-white/45">{finance.payoutRequestsEnabled ? payoutSummary ? `Latest request: ${payoutSummary.status} · ${payoutSummary.amount} ${payoutSummary.currency}.` : 'Payout requests are available when a reviewed destination and eligible balance exist.' : 'Payout requests are currently locked by the operational gate. No provider withdrawal will be initiated from this Studio.'}</p></div></div></div></>}</article></section>

    <section className="mt-8 rounded-2xl border border-white/[0.08] bg-white/[0.02] p-5 sm:p-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex items-center gap-2 text-primary"><Library className="h-5 w-5" /><h2 className="text-lg font-black text-white">Watch library pulse</h2></div><p className="mt-1 text-xs leading-relaxed text-white/45">Your latest creator uploads and official YouTube embeds. Only ready releases are visible in public Watch discovery.</p></div><Link href="/dashboard/watch" className="inline-flex min-h-10 w-fit items-center gap-1.5 text-xs font-black text-primary hover:text-white">Manage Watch <ArrowUpRight className="h-3.5 w-3.5" /></Link></div>{videosQuery.isLoading ? <div className="flex min-h-32 items-center justify-center"><Loader2 className="h-6 w-6 animate-spin text-primary" /></div> : videosQuery.isError ? <div className="mt-5 rounded-xl border border-red-300/20 bg-red-400/[0.05] p-4 text-sm text-red-100"><p className="font-black">Watch inventory is temporarily unavailable</p><button type="button" onClick={() => videosQuery.refetch()} className="mt-3 inline-flex min-h-10 items-center rounded-xl border border-red-200/25 bg-red-200/[0.08] px-4 text-xs font-black text-red-50 transition hover:bg-red-200/[0.14]">Retry Watch inventory</button></div> : videos.length ? <div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{videos.slice(0, 6).map((video) => <article key={video.id} className="flex min-w-0 items-center gap-3 rounded-xl border border-white/[0.07] bg-black/20 p-3"><div className="flex aspect-video w-20 shrink-0 items-center justify-center overflow-hidden rounded-lg bg-white/[0.04]">{video.thumbnailUrl ? <img src={video.thumbnailUrl} alt="" className="h-full w-full object-cover" /> : <Clapperboard className="h-5 w-5 text-white/25" />}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-black text-white">{video.title}</p><div className="mt-1 flex flex-wrap items-center gap-2 text-[10px] font-bold uppercase tracking-wider"><span className={`rounded px-1.5 py-0.5 ${video.uploadStatus === 'ready' ? 'bg-emerald-300/10 text-emerald-200' : video.uploadStatus === 'errored' ? 'bg-red-400/10 text-red-200' : 'bg-amber-300/10 text-amber-100'}`}>{video.uploadStatus}</span><span className="text-white/35">{video.viewCount.toLocaleString()} views</span></div></div></article>)}</div> : <div className="mt-5 rounded-xl border border-dashed border-white/[0.1] p-5 text-sm text-white/40">No Watch releases have been added. Publish a FastPix upload or an official, rights-cleared YouTube embed from the Watch manager.</div>}</section>

    <section className="mt-8 flex gap-3 rounded-2xl border border-amber-300/15 bg-amber-300/[0.045] p-4 text-xs leading-relaxed text-amber-100/80"><CircleAlert className="h-4 w-4 shrink-0 text-amber-200" /><p><b className="font-black text-amber-100">Operational boundary:</b> Studio reflects real platform records and routes to existing tools. Customer wallet custody, scheduled payout requests, provider withdrawals, and ad delivery remain hard-disabled and cannot be enabled here.</p></section></main>;
}
